<header>
    <nav class="navbar navbar-expand-lg " >
        <div class="container-fluid" >
            <img src="{{ asset('img/logo.png') }}" alt="">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <img src="{{ asset('images/logo eni.png') }}" alt="" width="80px">
                    <ul class="navbar-nav">

                        <li class="nav-item">
                            <a class="nav-link text-light mt-3" href="{{ path('app_sortie_index') }}">Accueil
                                <span class="visually-hidden">(current)</span>
                            </a>
                        </li>
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link text-light mt-3" href="{{ path('app_logout') }}">déconnexion
                                    <span class="visually-hidden">(current)</span>
                                </a>
                            </li>

                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link text-light mt-3" href="{{ path('app_login')}}">Connexion
                                    <span class="visually-hidden">(current)</span>
                                </a>
                            </li>

                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <a class="nav-link text-light mt-3" href="{{ path('admin')}}">Espace Admin
                                    <span class="visually-hidden">(current)</span>
                                </a>
                            </li>
                        {% endif %}
                    </ul>
                </div>


                        {% if app.user %}
                            <a href="{{ path('app_sortie_new') }}" id="creation" class="btn  btn-primary m-3">Créer Sortie</a>
                            <ul>
                                <li class="nav-item list-unstyled">
                                    <a class="nav-link text-light " href="">{{ app.user.prenom ~ ' ' ~ app.user.nom }}   &nbsp;&nbsp;   <img
                                                src="{{ asset('images/profil.png') }}" alt="" height="30">
                                        <span class="visually-hidden">(current)</span>
                                    </a>
                                    <ul>
                                        <li><a href="{{ path('app_user_show', {'id': app.user.id}) }}">Mon profil</a></li>
                                        <li><a href="{{ path('app_logout') }}">Deconnexion</a></li>

                                    </ul>
                                </li>
                            </ul>
                        {% endif %}
            </div>
    </nav>
</header>