{{ form_start(form) }}



    {{ form_row(form.nom) }}
    {{ form_row(form.dateHeureDebut) }}
    {{ form_row(form.dateLimiteInscription) }}
    {{ form_row(form.duree) }}
    {{ form_row(form.nbInscriptionMax) }}
    {{ form_row(form.infosSortie) }}
<div class="mb-3 row">
    <label class="col-form-label col-sm-2" for="select-ville">Villes:</label>
    <div class="col-sm-10">
        <select class="form-control form-select " id="select-ville" name="villes" id="">
            <option value="">Choisir une ville</option>
            {% for ville in villes %}
                <option value="{{ ville.id }}">{{ ville.nom }}</option>
            {% endfor %}
        </select>
    </div>
</div>

<!--<div class="mb-3 row">
    <label class="col-form-label col-sm-2" name="lieu" for="select-lieu">Lieu:</label>
    <div class="col-sm-10">
        <select class="form-control form-select " id="select-lieu" name="lieu" >
            <option value="">Choisir un lieu</option>
            {#
            {% for lieu in lieux %}
                <option name="lieu" value="{{ lieu.id }}">{{ lieu.nom }}</option>
            {% endfor %} #}
        </select>
    </div>
</div>
-->
{{ form_row(form.lieu ,{ 'id': 'select_lieu' }) }}
{{ form_row(form.isPublished) }}
<a class=" btn btn-primary m-3" id="ajouterLieu" href="{{ path('app_lieu_new') }}"> Ajouter un lieu</a>

<a class=" btn btn-primary m-3" href="#"> annuler</a>

<button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
<!-- Modale pour l'ajout de lieu -->
<div class="modal fade" id="ajoutLieuModal" tabindex="-1" role="dialog" aria-labelledby="ajoutLieuModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="ajoutLieuModalLabel">Ajouter un lieu</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                {{ render(controller('App\\Controller\\LieuController::new')) }}
            </div>
        </div>
    </div>
</div>

{{ form_end(form) }}
